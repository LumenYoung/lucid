# Lucid Docs

This directory is the primary human-facing documentation surface for Lucid.

Start here:

- `deployment.md` for service deployment and public MCP exposure
- `maintenance.md` for operator-side rebuild and re-embed workflows
- `runtime-notes.md` for Graphiti/FalkorDB runtime constraints and Lucid-specific behavior
- `agents/README.md` for Codex and OpenCode integration
- `agents/shared-instruction.md` for the shared Lucid agent instruction

Historical design notes may also live here when they are still useful as working context.
